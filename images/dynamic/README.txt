README

In order to make dynamic materials work properly, each object with a dynamic material must have a separate image that is dynamically changed by code.
To keep things tidy, all these images are stored here, and named:
D_<object name>.png
